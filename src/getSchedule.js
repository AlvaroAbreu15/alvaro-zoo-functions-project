// const { species } = require('../data/zoo_data');
const data = require('../data/zoo_data');

function getSchedule(scheduleTarget) {

  // const keys = scheduleTarget;
  // const keys2 = scheduleTarget.toLowerCase();
  // const keysAnimal = species.map((i) => i.name);

  // if (Object.keys(exp).includes(scheduleTarget)) {
  //   return `${keys}: ${exp[keys2]}`;
  // }
  // if (keysAnimal.includes(scheduleTarget)) {
  //   return species.find((e) => e.name === scheduleTarget).availability;
  // }
  // if ((scheduleTarget === undefined) || (scheduleTarget !== Object.keys(exp))) {
  //   return exp;
  // }
}

module.exports = getSchedule;
